# Required in production. Create a Blob store in the Vercel dashboard
# (Storage -> Blob) and this variable is added to the project for you.
# Without it the app falls back to temporary local disk storage, which is
# fine for `npm run dev` but loses everything on restart.
BLOB_READ_WRITE_TOKEN=
